---
driver:
  name: proxy
  host: localhost
  reset_command: "exit 0"
  port: 5985
  username: kitchen
  password: Pass@word1

provisioner:
  # salt_install: bootstrap
  # salt_bootstrap_options: -pythonVersion 3 -version 3002.5
  salt_bootstrap_url: bootstrap-salt.ps1
  init_environment: ''

platforms:
  - name: windows-2019
  # - name: windows-2016

suites:
  - name: latest-windows
    provisioner:
      # salt_version: latest
      # salt_version: 3002.5
      # salt_bootstrap_options: -MP stable %s
      salt_bootstrap_options: -pythonVersion 3 -version 3002.5

verifier:
  command: pytest --cache-clear -v -s -ra --log-cli-level=debug -k 'not test_ping' tests/integration/
