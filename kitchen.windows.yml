---
driver:
  name: proxy
  host: localhost
  reset_command: "exit 0"
  port: 5985
  username: kitchen
  password: Password1

provisioner:
  # salt_install: bootstrap
  # salt_bootstrap_options: -pythonVersion 3 -version 3002.5
  # salt_bootstrap_url: bootstrap-salt.ps1
  salt_bootstrap_url: D:/a/salt-bootstrap/salt-bootstrap/bootstrap-salt.ps1
  # salt_bootstrap_url: https://raw.githubusercontent.com/saltstack/salt-bootstrap/develop/bootstrap-salt.ps1
  init_environment: ''
  # install_after_init_environment: false
  # run_salt_call: true
  

platforms:
  - name: windows-2019
  # - name: windows-2016

verifier:
  command: pytest --cache-clear -v -s -ra --log-cli-level=debug --deselect tests\integration\test_installation.py::test_ping tests\integration\
  # command: pytest --cache-clear -v -s -ra --log-cli-level=debug -k 'not test_ping' tests\integration\
  # command: pytest --cache-clear -v -s -ra --log-cli-level=debug -k 'not test_ping' D:\a\salt-bootstrap\salt-bootstrap\tests\integration\
